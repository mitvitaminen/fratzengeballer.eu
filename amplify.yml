version: 1
frontend:
  phases:
    preBuild:
      commands:
        - rvm install 3.2.2
        - rvm use 3.2.2
        - bunle install
    build:
      commands:
        - bundle exec jekyll build
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []
