version: 1
frontend:
  phases:
    preBuild:
      commands:
        - rvm install 4.0.6
        - rvm use 4.0.6
        - bunle install
    build:
      commands:
        - bundle exec jekyll build
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []
